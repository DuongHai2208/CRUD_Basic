server:
  port: 8080
  servlet:
    context-path: /demo

spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/test"
    username: root
    password: hailun123
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true